import { StyleSheet, Text, View, SafeAreaView } from 'react-native';
import React from 'react';

const ViewBudgetsScreen = () => {
	return (
		<SafeAreaView>
			<Text>ViewBudgetsScreen</Text>
		</SafeAreaView>
	);
};

export default ViewBudgetsScreen;

const styles = StyleSheet.create({});
